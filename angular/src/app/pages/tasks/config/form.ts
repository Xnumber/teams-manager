import type { Editing } from "devextreme/ui/data_grid";

export const editingPopupProps: Editing['popup'] = {
    width: '100%',
    height: '100%',
    showTitle: true,
    title: "工作",
    dragEnabled: true,
}